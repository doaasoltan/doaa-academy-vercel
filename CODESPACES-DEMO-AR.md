# رابط المنصة الجاهز عبر GitHub Codespaces (5 دقائق، مجاناً)

> الطريقة دي بتشغّل **نسختك الخاصة** من المنصة على سيرفرات GitHub،
> وتديكي **رابط عام** تفتحيه من أي جهاز — بدون تثبيت أي برامج وبدون قاعدة بيانات.

## قبل ما تبدئي

- حساب GitHub (هو نفسه اللي عليه المشروع `doaa-academy-vercel`).
- ادمجي التحديثات الأول: افتحي صفحة المشروع ← تبويب **Pull requests** ←
  افتحي طلب الدمج ← زر **Merge pull request** ← **Confirm merge**.
  (أو تخطي الدمج وأنشئي الـ Codespace من فرع `arena/01a085a2-doaa-academy-vercel` مباشرة).

## الخطوات (مرة واحدة فقط)

1. افتحي صفحة المشروع على GitHub.
2. اضغطي الزر الأخضر **Code** ← تبويب **Codespaces** ← زر **+** (New codespace).
3. انتظري 3–5 دقائق أول مرة (تثبيت تلقائي + تشغيل تلقائي للمنصة).
4. من الأسفل افتحي تبويب **PORTS** ← ستجدي المنفذ **3000** بعنوان *Academy Demo* ←
   اضغطي أيقونة **المتصفح/الكرة الأرضية** (Open in Browser).
5. اتفتحت المنصة! سجّلي الدخول بأي حساب:

| الحساب | البريد | كلمة المرور |
|---|---|---|
| 👩‍💼 المعلمة | `admin@demo.test` | `Admin1234` |
| 👩‍🎓 الطالبة | `student@demo.test` | `Demo1234` |

## مشاركة الرابط مع شخص آخر

الرابط يعمل لصاحبة الحساب فقط افتراضياً. لمشاركته:

1. تبويب **PORTS** ← كليك يمين على المنفذ **3000**.
2. **Port Visibility** ← اختاري **Public**.
3. انسخي رابط **Forwarded Address** (شكله `https://....github.dev`) وأرسليه.

## الاستخدام اليومي

- كل مرة تدخلي Codespaces من نفس المكان، ستجدي بيئتك محفوظة — اضغطي عليها فقط.
- الخادم يعمل تلقائياً عند تشغيل البيئة. لو توقف لسبب ما، افتحي الـ Terminal واكتبي:

```bash
npx tsx server/_core/index.ts
```

- لإيقاف البيئة وتوفير الحصة المجانية: من صفحة الـ Codespace ← **Stop codespace**.
  (الحصة المجانية: 120 ساعة شهرياً — تكفي للتجربة والاستخدام الخفيف).

## استكشاف الأخطاء

| المشكلة | الحل |
|---|---|
| صفحة المنفذ 3000 فارغة/خطأ | انتظري دقيقة ثم حدّثي — الخادم يستغرق ~30 ثانية أول مرة |
| `command not found: tsx` | نفّذي `npm install` أولاً ثم أعيدي تشغيل الخادم |
| نسيت بيانات الدخول | المعلمة: `admin@demo.test / Admin1234` — الطالبة: `student@demo.test / Demo1234` |
| تريدين تصفير البيانات التجريبية | احذفي `uploads/.demo-data.json` وأعيدي تشغيل الخادم |

## ملحوظة عن الرابط الدائم

رابط Codespaces ممتاز للتجربة والعرض، لكنه يتوقف عند إيقاف البيئة.
للرابط الدائم (موقع ثابت باسم نطاق) اتبعي دليل **`FREE-DB-SETUP-AR.md`**
(قاعدة TiDB المجانية + نشر Vercel).
