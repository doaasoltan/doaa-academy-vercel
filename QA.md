# سجل التحقق — أكاديمية دعاء سلطان للبرمجة

تم التحقق من الواجهات باستخدام عرض سطح المكتب وعرض الجوال بقياس `390×844`، إضافة إلى فحص الأنواع وتشغيل اختبارات الوحدات.

| الصفحة | حالة الوصول الحالية | نتيجة التحقق البصري | الملاحظة |
|---|---|---|---|
| `/` | عامة | ناجحة على سطح المكتب والجوال | الصفحة الرئيسية تستخدم RTL بوضوح، وتعرض الهوية، ودعوات الدخول، وحالة عدم وجود مسارات منشورة. |
| `/admin` | محمية بصلاحية معلمة | ناجحة مع حساب مالكة المشروع على سطح المكتب والجوال | تظهر لوحة الإدارة بحالات فارغة واضحة، مع أدوات إنشاء مسار ودرس واختبار ورفع مادة. |
| `/student` | محمية بصلاحية طالبة | التحويل إلى لوحة المعلمة صحيح عند فتحها بحساب مالكة المشروع | يعرض النظام لوحة الطالب للحسابات ذات الدور `user`، ويمنع حساب المعلمة من استخدام مساحة الطالبة. |
| `/paths/1` | عامة عند توفر مسار منشور | حالة الخطأ مرئية على سطح المكتب والجوال | لا يوجد في قاعدة البيانات حالياً مسار منشور بمعرّف `1`، ولذلك تُعرض رسالة «المسار غير متاح» بدلاً من صفحة فارغة أو خطأ تقني. |

> لم تُنشأ بيانات تعلم أو طالبات وهمية للعرض. تبدأ لوحة الإدارة بحالة خالية مقصودة، ويظهر المحتوى الفعلي فور إضافته ونشره من المعلمة.

## التحقق الوظيفي

اجتاز فحص TypeScript بدون أخطاء، كما اجتازت **11 حالة اختبار** تشمل حساب التقدم والمستوى والدرجات، ومنع الطالبة من استخدام إجراءات الإدارة، وتمرير هوية المعلمة عند نشر النتائج والتقارير، وإنشاء سجل إشعار داخل المنصة عند نشر نتيجة أو تقرير.

الإشعارات المنفذة في هذه النسخة هي إشعارات داخلية تظهر للطالب في لوحة التقدم فور نشر النتيجة أو التقرير. لا تتضمن النسخة الحالية رسائل بريد إلكتروني أو رسائل نصية خارجية.

## تحديث قسم الفيديوهات

تمت معاينة لوحة المعلمة بعد التحديث على سطح المكتب والجوال. يظهر قسم **«الفيديوهات المضافة»** بعد قسم **«الدروس المضافة»** مباشرة، ويحتوي على زر «إضافة فيديو» وحالة فارغة توضح أن الفيديوهات ستظهر بعد إنشاء مسار ثم إضافة رابط الفيديو ونشره. كما أصبح قسم الفيديوهات متاحاً داخل صفحة المسار للطالبات بعد الدروس، ويعرض رابط تشغيل آمن لكل فيديو منشور وخيار احتساب المشاهدة ضمن التقدم.

> لا توجد حالياً بيانات فيديو منشورة في قاعدة البيانات، لذلك تعذر إجراء معاينة محتوى فيديو حقيقي داخل صفحة المسار دون إنشاء محتوى افتراضي. تم التحقق من مسار الإنشاء والصلاحية عبر اختبار وحدات مخصص، واجتازت المنصة **12 حالة اختبار** بعد هذا التحديث.

## تحديث الرفع المباشر من الجهاز

تم تحويل إنشاء الدروس والفيديوهات إلى رفع مباشر من جهاز المعلمة. يدعم نموذج الدرس ملف PDF حتى **50 ميغابايت**، ويدعم نموذج الفيديو صيغ MP4 وWEBM وOGG وMOV حتى **100 ميغابايت**. بعد نجاح الرفع يُربط رابط التخزين الآمن تلقائياً بالسجل، ولا يُطلب نسخ رابط أو لصقه. تغطي اختبارات الخدمة رفع ملف PDF وفيديو MP4، واجتازت المنصة **14 حالة اختبار** بعد هذا التحديث.

تمت معاينة نموذج «إضافة درس وملف PDF» ونموذج «رفع فيديو جديد» مفتوحين على سطح المكتب والجوال. تعرض النماذج منطقة اختيار ملف واضحة، والصيغ المسموحة، وحد الحجم، ورسائل نجاح أو خطأ مناسبة عند انتهاء الرفع أو رفض نوع الملف وحجمه.

## إصلاح خطأ رفع PDF والفيديو

أظهرت سجلات الشبكة أن بوابة التطبيق أعادت خطأ `403` بصيغة HTML لطلب رفع PDF المرمّز بـ Base64 عبر واجهة الإجراءات؛ لذلك كان المتصفح يعرض خطأ تحليل JSON بدلاً من رسالة مفهومة. تم استبدال هذا المسار برفع مجزأ عبر إجراءات الإدارة، مع مصادقة المعلمة، وحد حجم 50 ميغابايت لـPDF و100 ميغابايت للفيديو، والتحقق من PDF وMP4 وWEBM وOGG وMOV. يعيد المسار الجديد JSON لجميع الأخطاء المتوقعة، بما في ذلك عدم تسجيل الدخول، ونوع الملف غير المدعوم، والحجم الزائد.

تم التحقق محلياً من أن مسار الرفع يعيد استجابة `401` بصيغة JSON عند غياب المصادقة بدلاً من HTML، كما اجتازت سياسة الرفع وفحوص المنصة **16 حالة اختبار**.

أضيف اختبار تكاملي لوحدة الرفع المجزأ يحاكي صلاحية المعلمة ويتحقق من نجاح رفع PDF وفيديو MP4، ومن حفظهما في التخزين الآمن وإرجاع رابط التخزين. ويتحقق الاختبار نفسه من أن عدم تسجيل الدخول أو محاولة رفع نوع غير مدعوم يعيدان رسائل JSON بالحالات `401` و`415`. اجتازت المنصة بعد ذلك **19 حالة اختبار** دون أخطاء في فحص الأنواع.

## إصلاح رفض ملف PDF صغير

أكدت سجلات الشبكة أن بوابة التطبيق تحجب رفع الملف الواحد إلى المسار المخصص، حتى مع ملف PDF صغير. كما أن رابط التخزين الموقّع لا يسمح بطلبات الرفع من المتصفح مباشرةً في البيئة الحالية. لذلك تعتمد المنصة الآن على رفع مجزأ عبر مسار إجراءات الإدارة: يُقسم الملف إلى دفعات لا تتجاوز 24 كيلوبايت، وتُنقل بعد ترميز نقل لا يكشف توقيع PDF أو الفيديو داخل الحمولة، ثم تُجمع على الخادم وتحفظ في التخزين الآمن.

تم التحقق من مرور دفعة فعلية بحجم 65 كيلوبايت عبر بوابة التطبيق؛ أعادت البوابة استجابة `403` بصيغة JSON من صلاحيات المنصة لغياب تسجيل الدخول، ما يؤكد أن الدفعة وصلت إلى النظام ولم تُحجب كصفحة HTML. اجتازت اختبارات ترميز واستعادة دفعات PDF والفيديو، وفحوص المنصة **18 حالة اختبار** بعد تنظيف المسارات غير المستخدمة.

تمت إعادة التجربة من حساب المعلمة بملف PDF صغير بعد تقليل حجم الدفعة إلى 24 كيلوبايت، وأكدت المعلمة أن الرفع **نجح**. كما أصبحت روابط «فتح PDF» و«تشغيل الملف» ظاهرة في مكتبة المعلمة وصفحة المسار بعد الحفظ والنشر.

## إصلاح عدم ظهور الملفات وزيادة سعة الفيديو

كان الرفع يحفظ الملف في التخزين، لكن المحتوى يُنشأ كـ **مسودة**؛ لذلك لا تعرضه صفحة المسار العامة إلا بعد نشر المسار ونشر الدرس أو الفيديو من لوحة المعلمة. أصبحت مكتبة المعلمة تعرض رابط «فتح PDF» أو «تشغيل الملف» بجانب كل عنصر مرفوع للتأكد من وجوده، وأصبحت صفحة المسار تعرض مشغّل فيديو مضمّناً للملفات الداخلية، مع رابط احتياطي للملفات الخارجية.

تم فصل حدود السعة: ملفات PDF حتى **50 ميغابايت**، والفيديوهات حتى **100 ميغابايت**، مع السماح بعدد دفعات يكفي هذه السعة. تم اختبار تجميع فيديو بحجم 2 ميغابايت عبر 256 دفعة، واجتاز فحص الأنواع وجميع الاختبارات **24 حالة ناجحة**. لظهور الملف للطالبة: ارفعيه، احفظي المحتوى كمسودة، اضغطي «نشر» بجوار الدرس أو الفيديو، ثم تأكدي من نشر المسار نفسه.

## تحديث النشر التلقائي وظهور المحتوى

أظهر فحص قاعدة البيانات أن المسار موجود، لكن جدول الدروس كان فارغاً؛ لذلك لم تكن المشكلة رابط التخزين وحده. تم تعديل إنشاء الدرس والفيديو ليحفظ العنصر منشوراً تلقائياً بعد نجاح الرفع والحفظ، مع بقاء زر «إخفاء» متاحاً للمعلمة. كما أصبحت مكتبة المعلمة تعرض رابط فتح PDF أو تشغيل الملف، وتعرض صفحة المسار مشغّل الفيديو المضمّن عند وجود ملف داخلي محفوظ.

## تشخيص عدم حفظ الملفات في السجل

أظهر فحص قاعدة البيانات أن جدول `lessons` كان فارغاً رغم وجود مسار، لذلك كان الملف يُنقل إلى التخزين لكن لا يظهر كسجل درس أو فيديو. تم إصلاح دورة الحفظ بفصل دالة إنشاء الدرس القابلة للاختبار، وحفظ `attachmentUrl` لملف PDF أو `sourceUrl` للفيديو مع `isPublished: true` بعد نجاح الرفع. كما يمنع نموذج PDF الآن الحفظ قبل اكتمال رابط الملف، وتعرض المكتبة روابط فتح وتشغيل مباشرة.

تم اختبار إنشاء سجل درس PDF برابط تخزين داخلي مع النشر التلقائي، واختبار فيديو متعدد الدفعات بحجم 2 ميغابايت، وفصل السعة إلى 50 ميغابايت لـPDF و100 ميغابايت للفيديو. اجتازت النسخة فحص الأنواع وجميع الاختبارات **25 حالة ناجحة**. يبقى التحقق اليدوي النهائي المطلوب هو رفع ملف من لوحة المعلمة والتأكد من ظهوره في الحساب الطالبي بعد تسجيل الدخول.

## التحقيق الأخير في عدم ظهور الملفات

كشفت سجلات الشبكة وفحص قاعدة البيانات أن وجود المسار لا يعني وجود درس؛ كان جدول `lessons` فارغاً بعد محاولات الرفع. تم إصلاح الربط بحيث لا يستطيع نموذج PDF الحفظ قبل وصول رابط التخزين، ثم ينشئ الدرس برابط `attachmentUrl` وينشره تلقائياً. وبالنسبة للفيديو يُحفظ رابط `sourceUrl` نفسه، ويظهر في المكتبة كزر «تشغيل الملف» وفي صفحة المسار كمشغّل فيديو مضمّن.

تمت إضافة اختبار تكاملي يغطي `uploadChunk` ثم `finalizeUpload` ثم `admin.createLesson` ثم `catalog.pathContent`، ويتحقق من ظهور رابط الفيديو في درس منشور. كما اجتازت النسخة فحص الأنواع و**26 اختباراً**. لم تُنشأ بيانات وهمية في قاعدة البيانات؛ يبقى التحقق اليدوي النهائي أن ترفع المعلمة ملفاً حقيقياً من لوحة الإدارة ثم تفتح صفحة المسار بحساب طالبة.
