# إصلاح رفع الملفات محلياً

تم تغيير رفع PDF والفيديو إلى رفع ثنائي مباشر عبر `/api/local-upload` بدلاً من إرسال الملف بصيغة Base64 عبر tRPC. هذا يمنع مشاكل حدود حجم JSON ويجعل زر «حفظ ونشر» يتفعل بعد اكتمال الرفع.

- PDF: حتى 50 MB
- Video: حتى 100 MB
- التخزين: `uploads/academy/`
- يحتاج المستخدم إلى جلسة أدمن محلية.

بعد فك الضغط، انسخي `.env` من النسخة التي تعمل لديك إن لم يكن موجوداً، ثم شغلي:

```bash
pnpm install
pnpm db:push
pnpm dev
```
