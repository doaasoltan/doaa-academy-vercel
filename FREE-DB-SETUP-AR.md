# ربط المشروع بقاعدة بيانات مجانية (TiDB Cloud) — دليل خطوة بخطوة

> الخلاصة: قاعدة **TiDB Cloud Starter** مجانية (بدون بطاقة بنكية)، متوافقة مع MySQL
> بدون أي تعديل في الكود، وتكفي الأكاديمية (5GB تخزين + 50 مليون طلب شهرياً).
> والفيديوهات بعدد **غير محدود** عبر روابط YouTube (مشروحة في الأسفل).

## المتطلبات

- حساب GitHub (لرفع المشروع وربطه بـ Vercel).
- حساب TiDB Cloud مجاني (بدون بطاقة بنكية).
- حساب Vercel مجاني.
- على جهازك: Node.js 20+ ثم `npm install` داخل مجلد المشروع.

---

## الخطوة 1: إنشاء قاعدة البيانات المجانية

1. سجّلي في [tidbcloud.com](https://tidbcloud.com) (يمكن التسجيل بحساب Google).
2. أنشئي Cluster جديد من نوع **Starter** (كان اسمه Serverless سابقاً) — الخطة المجانية.
3. اختاري أقرب منطقة (مثلاً Frankfurt `eu-central-1` مناسبة لمصر وأوروبا).
4. من صفحة الـ Cluster أنشئي قاعدة بيانات باسم `doaa_academy`.
5. أنشئي مستخدم SQL (مثلاً `academy`) واحفظي كلمة المرور — تظهر مرة واحدة فقط.
6. **مهم:** اسمحي بالاتصال من أي مكان، لأن Vercel لا يستخدم IP ثابتاً:
   من إعدادات الـ Cluster ← السماح بالوصول (Allow Access) ← أضيفي `0.0.0.0/0`.

## الخطوة 2: تجهيز رابط الاتصال

من صفحة الـ Cluster ← زر **Connect** انسخي بيانات الاتصال، ثم ركّبي الرابط بهذا الشكل:

```text
mysql://USER:PASSWORD@HOST:4000/doaa_academy?sslaccept=strict
```

- `HOST` مثل: `gateway01.eu-central-1.prod.aws.tidbcloud.com`
- المنفذ دائماً `4000`.
- `?sslaccept=strict` **إجباري** لأن TiDB تشترط اتصالاً مشفراً.
- إذا كانت كلمة المرور تحتوي رموزاً خاصة، رمّزيها (URL-encode):
  `@` ← `%40` ، `#` ← `%23` ، `/` ← `%2F` ، `:` ← `%3A`.

## الخطوة 3: إنشاء الجداول من جهازك

```bash
# داخل مجلد المشروع
npm install
cp .env.example .env
# افتحي ملف .env وضعي رابط TiDB في DATABASE_URL (لا ترفعي هذا الملف أبداً على GitHub)

npm run db:push
```

الأمر `db:push` ينشئ كل الجداول (المسارات، الدروس، الاختبارات، النتائج،
التقارير، الإشعارات، المستخدمين + عمود كلمة المرور).

## الخطوة 4 (اختياري): بيانات تجريبية

```bash
npm run db:seed
```

ينشئ مساراً تجريبياً منشوراً (مقال + فيديو يوتيوب + مورد) وحساب طالبة:

```text
student@demo.test / Demo1234
```

يمكن تغييرهما من `SEED_STUDENT_EMAIL` و`SEED_STUDENT_PASSWORD`.
الأمر آمن التكرار: لا يكرر أي بيانات موجودة.

## الخطوة 5: النشر على Vercel

1. ارفعي المشروع على GitHub ثم استورديه في Vercel (Import Project).
2. إعدادات البناء:
   - Build Command: `npm run build:vercel`
   - Output Directory: `dist/client`
3. أضيفي متغيرات البيئة (Environment Variables):

| المتغير | القيمة |
|---|---|
| `DATABASE_URL` | رابط TiDB من الخطوة 2 |
| `JWT_SECRET` | نص عشوائي طويل (ولّديه بـ `openssl rand -hex 32`) |
| `ADMIN_EMAIL` | بريد المديرة |
| `ADMIN_PASSWORD` | كلمة مرور قوية (8 أحرف على الأقل) |
| `ADMIN_NAME` | مديرة الأكاديمية |
| `LOCAL_AUTH` | `false` |
| `BLOB_READ_WRITE_TOKEN` | يُضاف تلقائياً عند ربط Blob Store (الخطوة 6) |

4. اضغطي **Deploy**.

## الخطوة 6: تفعيل رفع الملفات (Vercel Blob)

من لوحة تحكم Vercel ← مشروعك ← **Storage** ← أنشئي **Blob Store** واربطيه بالمشروع،
ثم أعيدي الـ Deploy. تُستخدم لملفات PDF والفيديوهات المرفوعة من الجهاز
(حتى 50MB للـ PDF و100MB للفيديو).

## الخطوة 7: أول دخول وتشغيل

1. افتحي رابط الموقع وسجّلي الدخول ببريد المديرة وكلمة مرورها
   (يُنشأ حسابها تلقائياً عند أول تشغيل).
2. أنشئي مساراً من لوحة المعلمة وانشريه.
3. أضيفي فيديو: ارفعي ملفاً من جهازك **أو** الصقي رابط يوتيوب.
4. سجّلي خروجاً وادخلي بحساب الطالبة التجريبية للتأكد من ظهور المحتوى.

---

## فيديوهات بعدد غير محدود (مجاناً)

- **روابط YouTube = فيديوهات غير محدودة ومجانية تماماً.** ارفعي الفيديو على
  يوتيوب كـ **غير مدرج (Unlisted)** ثم الصقي رابطه في نموذج «إضافة فيديو» —
  سيظهر مشغّل مدمج داخل صفحة المسار (يدعم روابط `watch` و`youtu.be` و`shorts` و`live`).
- **الرفع من الجهاز** محدود بحصة Vercel Blob المجانية (1GB تخزين **و10GB
  انتقالات/شهر**). الانتقالات هي العامل الحاسم: كل مشاهدة لطالبة لفيديو مرفوع
  تضيف حجم الفيديو كاملاً إليها (فيديو 100MB × 100 طالبة = كل الحصة!). عند
  تجاوز الحد يوقف Vercel المخزن مؤقتاً حتى بداية الشهر التالي. لذا الرفع من
  الجهاز مناسب لملفات PDF والفيديوهات القصيرة المهمة، أما الفيديوهات الطويلة
  والمكتبة الكبيرة فالأفضل يوتيوب.
- قاعدة البيانات نفسها لا تخزن الفيديو، بل رابط التشغيل فقط — لذلك عدد
  الفيديوهات لا يتأثر بحجم القاعدة المجانية.

## استكشاف الأخطاء

| المشكلة | الحل |
|---|---|
| `Access denied` أو تعذر الاتصال | تأكدي من السماح `0.0.0.0/0` ومن ترميز رموز كلمة المرور |
| `Unknown column 'passwordHash'` | نفّذي `npm run db:push` لإضافة آخر migration |
| صفحة بيضاء بعد تسجيل الدخول | تأكدي من `JWT_SECRET` (32 حرفاً على الأقل) ومن `DATABASE_URL` في Vercel |
| فشل رفع الملفات | تأكدي من ربط Blob Store وإعادة الـ Deploy |
| `No blob credentials found` في السجلات | اضبطي `BLOB_READ_WRITE_TOKEN` أو `BLOB_STORE_ID` ثم Re-deploy (تفاصيل في VERCEL-DEPLOY-AR.md) |
| تنبيه «You have reached your usage limits for this store» | المخزن موقوف مؤقتاً — راجعي قسم الاستكشاف في VERCEL-DEPLOY-AR.md |
| نفاد حصة TiDB المجانية | تتوقف الاستجابة مؤقتاً **بدون رسوم** حتى بداية الشهر التالي |

## بدائل مجانية أخرى

- **Aiven**: خطة مجانية صغيرة لـ MySQL (تحتاج بطاقة أحياناً).
- **Neon / Supabase**: خطط Postgres مجانية وسخية، لكنها تتطلب تحويل المشروع
  من MySQL إلى Postgres (تعديل كبير في الـ schema) — غير موصى به الآن.
