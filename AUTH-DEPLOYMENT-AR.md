# تسجيل الدخول الحقيقي والنشر على الإنترنت

تم تحويل تسجيل الدخول من الدخول المحلي إلى بريد إلكتروني + كلمة مرور. كلمات المرور لا تُخزّن كنص صريح؛ يتم تخزين تجزئة باستخدام scrypt. الجلسة تستخدم Cookie آمنة وHttpOnly وJWT موقّعًا بـ JWT_SECRET.

## حساب الأدمن

ضع في ملف `.env`:

```env
ADMIN_EMAIL=admin@your-domain.com
ADMIN_PASSWORD=كلمة_مرور_قوية_جداً
ADMIN_NAME=مديرة الأكاديمية
```

عند تشغيل السيرفر يتم إنشاء حساب الأدمن تلقائيًا أو تحديث كلمة مروره إذا كان موجودًا. لا يتم توفير تسجيل أدمن عام للزوار.

## الطلاب

الطالبة تدخل من `/login` وتختار إنشاء حساب طالبة. الحساب يُحفظ في MySQL والدخول التالي يتم بالبريد وكلمة المرور.

## أول تشغيل

1. أنشئ قاعدة MySQL.
2. اضبط `DATABASE_URL` و`JWT_SECRET` وبيانات الأدمن.
3. نفذ `pnpm install`.
4. نفذ `pnpm db:push`.
5. نفذ `pnpm build`.
6. نفذ `pnpm start`.

## HTTPS

عند النشر استخدم HTTPS عبر Nginx أو مزود الاستضافة. التطبيق يقرأ `X-Forwarded-Proto` ويضبط Cookie `Secure` عند الاتصال عبر HTTPS.

## الملفات والفيديو

النسخة الحالية تخزن الرفع في مجلد `uploads`. على خادم واحد مع قرص دائم يمكن استخدامه، لكن في خدمات الاستضافة المؤقتة يجب ربط `uploads` بتخزين دائم أو Object Storage قبل الإنتاج.

## ملاحظات أمنية

- لا ترفع ملف `.env` إلى GitHub.
- غيّر `JWT_SECRET` وبيانات الأدمن قبل النشر.
- لا تستخدم `LOCAL_AUTH=true` في الإنتاج.
- خذ نسخًا احتياطية من MySQL ومجلد `uploads`.
